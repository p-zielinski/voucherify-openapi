Example script to generate markdown tables describing selected objects that use "$ref" from OpenAPI file.

Cli :
- `npm i` - install required dependencies
- `npm run start` - generate markdown files into `output` folder
